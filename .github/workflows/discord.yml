name: Ticket checker
on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check Tickets
        uses: mooyoul/melon-ticket-actions@v1.1.0
        with:
          product-id: 211960
          schedule-id: 100001
          seat-id: 10000
          slack-incoming-webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          message: '@everyone There’s a ticket available, QUICK!'
